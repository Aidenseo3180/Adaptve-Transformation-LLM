method: "simplified_plds"
model_path: "meta-llama/Llama-3.2-3B-Instruct"
dataset: "Open-Orca/SlimOrca"
dataset_column: "text"
batch_size: 4
max_length: 512
layers_to_skip: 4
dataset_size: 1000
dataset_subset: "train"
use_4bit: False
save_path: null

distances_path: "distances.pth"
num_A: 1
merge_consecutive: False

# Simplified PLDS parameters
learning_epochs: 5
learning_rate: 0.00005
warmup_steps: 100

tasks:
  boolq:
    fewshots: 0
  lambada_openai:
    fewshots: 0