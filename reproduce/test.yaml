method: "recycling"
model_path: "meta-llama/Llama-3.2-3B-Instruct"
dataset: "Open-Orca/SlimOrca"
dataset_column: "text"
batch_size: 4
max_length: 512
layers_to_skip: 4  # Not used but kept for compatibility
dataset_size: 1000
dataset_subset: "train"
use_4bit: False
save_path: null
similarity_threshold: 0.85  # Threshold for considering representations similar
min_recycling_gap: 3  # Minimum gap between source and recycled layer
distances_path: "distances.pth"

tasks:
  boolq:
    fewshots: 0
  lambada_openai:
    fewshots: 0